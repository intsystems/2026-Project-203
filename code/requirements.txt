# Everything the paper's tables and figures need.
#
#   pip install -r requirements.txt
#
# This file was empty, while REPRODUCE.md told readers to install from it.
#
# The counterexamples (Theorems 1-4, Figure 1) need only numpy + matplotlib and
# run on CPU in seconds; everything else needs torch.
#
# Nothing is pinned tighter than the minimum each feature requires. The results
# are not bitwise-reproducible across torch releases in any case (cuDNN kernel
# selection, bfloat16 rounding), so a pin would be a false promise; the version
# actually used is what the reproducibility checklist should record.

numpy>=1.21
torch>=1.11           # Tensor.mT, used by the Newton-Schulz iteration
torchvision>=0.12     # CIFAR-10 / MNIST
matplotlib>=3.5       # figures and notebooks only

# nanogpt/ (the modded-nanogpt speedrun port) additionally needs a CUDA build of
# torch and, for train_gpt.py only, FlashAttention-3 via `kernels`. It is not
# part of any paper table; see nanogpt/README.md.
