# What make_opt.py produces. Regenerate with:  python make_opt.py
#
# The compiled PDFs are tracked: they are the thing anyone reading the
# repository wants to look at, and the sources that make them are here beside
# them. The zips and the build directory are not, being one command away.
build/
.build-work/
signmuon_opt.zip
signmuon_opt_named.zip
signmuon_opt_final.zip
__pycache__/
